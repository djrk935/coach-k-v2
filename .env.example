# PostgreSQL with pgvector (create DB, then: psql $DATABASE_URL -f migrations/001_init.sql)
DATABASE_URL=postgresql://coach:coach@localhost:5432/coachk

# Chat brain (agent loop)
ANTHROPIC_API_KEY=sk-ant-...

# Ingestion — LlamaParse (table-aware PDF parsing) + OpenAI embeddings
LLAMA_CLOUD_API_KEY=llx-...
OPENAI_API_KEY=sk-...
